name: Deploy Application

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  lint:
    uses: ./.github/workflows/lint.yml

  test_unit:
    uses: ./.github/workflows/test_unit.yml

  test_e2e:
    uses: ./.github/workflows/test_e2e.yml

  # Heavy integration (Postgres + Elasticsearch + Chrome + live Candhis)
  # stays available as a manual workflow: test_integration.yml
